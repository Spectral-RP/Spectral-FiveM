var Rules = {
	
	contents: [
        { 
			title:'Rules 1', 
			texts:[
                {text:'1.1 rules rules rules rules'},
                {text:'1.2 rules rules rules rules rules rules rules rules'},
                {text:'1.3 rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules'}
            ]
		},
		{ 
			title:'Rules 2', 
			texts:[
                {text:'2.1 rules rules'},
                {text:'2.2 rules rules rules rules'},
                {text:'2.3 rules rules rules rules rules rules rules rules'},
                {text:'2.4 rules rules rules rules rules rules rules rules'}
            ]
		},
		{ 
			title:'Rules 3', 
			texts:[
                {text:'3.1 rules rules'},
                {text:'3.2 rules rules rules rules'},
                {text:'3.3 rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules'}
            ]
		},
		{ 
			title:'Rules 4', 
			texts:[
                {text:'4.1 rules rules'},
                {text:'4.2 rules rules rules rules'},
                {text:'4.3 rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules'},
                {text:'4.4 rulesrules rules rules rules rules rules rules'},
                {text:'4.5 rules rules rules rules'},
                {text:'4.6 rules rules rules rulerules rules rules rulessrules rules rules rrules rules rules rulesules'},
                {text:'4.7 rules rrules rrules rules rules rulrules rules rules rulesesrules rules rules rulesules rules rulesules rules rules'},
                {text:'4.8 rules rules rules rules rules rules rulesrules rules rules rulesrules'}
            ]
		},
		{ 
			title:'Rules 5', 
			texts:[
                {text:'5.1 rules rules'},
                {text:'5.2 rules rules rules rules'},
                {text:'5.3 rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules'},
                {text:'5.4 rulesrules rules rules rules rules rules rules'},
                {text:'5.5 rules rules rules rules'},
                {text:'5.6 rules rules rules rulerules rules rules rulessrules rules rules rrules rules rules rulesules'},
                {text:'5.7 rules rrules rrules rules rules rulrules rules rules rulesesrules rules rules rulesules rules rulesules rules rules'},
                {text:'5.8 rules rules rules rules rules rules rulesrules rules rules rulesrules'},
                {text:'5.9 rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules rules'},
                {text:'5.10 rulesrules rules rules rules rules rules rules'},
                {text:'5.11 rules rules rules rules'}
            ]
		},
		{ 
			title:'Rules 6', 
			texts:[
                {text:'6.1 rules rulesrulesrules'},
                {text:'6.2 rules rules rulruleses rules'},
                {text:'6.3 rules rules rules rrulesules rules rules rules rules rules rules rules rules rules rules rules rules'}
            ]
		}
    ],
};